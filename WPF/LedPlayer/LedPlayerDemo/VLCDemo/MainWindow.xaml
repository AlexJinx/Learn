<Window x:Class="VLCDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vlc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Title="MainWindow" Height="520" Width="228" Background="Black">
    <!--<Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="400"/>
                <RowDefinition Height="120"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <vlc:VideoView x:Name="Wrapper1" />
                <vlc:VideoView x:Name="Wrapper2" />
            </Grid>
            <Grid Grid.Row="1" Background="Gray">
                -->
    <!--<Button Height="100" Width="50" Margin="0,50,200,50" Click="Button_Click">重置
                    </Button>
                    <Button Height="100" Width="50" Margin="200,50,0,50" Click="Button_Click_1">播放</Button> -->
    <!--
            </Grid>
        </Grid>
    </Grid>-->
    <Grid>
        <vlc:VideoView x:Name="Wrapper1" Background="Black" />
        <vlc:VideoView x:Name="Wrapper2" Background="Black" />
    </Grid>
</Window>